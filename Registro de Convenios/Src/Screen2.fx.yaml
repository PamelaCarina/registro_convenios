Screen2 As screen:

    Label2 As label:
        Fill: =RGBA(209, 232, 178, 1)
        Text: |-
            =If(
                !IsBlank(Match(TrimEnds(Trim(TextInput1.Text)), "[a-z][a-z]").FullMatch) || //"xx"
                !IsBlank(Match(TrimEnds(Trim(TextInput1.Text)), "[a-z0-9]\.").FullMatch) || //"x."
                !IsBlank(Match(TrimEnds(Trim(TextInput1.Text)), "[a-z0-9] ").FullMatch) //"x "
            ,
                true
            )
        X: =504
        Y: =210
        ZIndex: =1

    TextInput1 As text:
        Default: =""
        X: =888
        Y: =354
        ZIndex: =2

    Button2 As button:
        OnSelect: =Set(coleccion_prueba,Match(TrimEnds(Trim(TextInput1.Text)), "[a-z][a-z] ").FullMatch);
        Text: ="Botón"
        X: =572
        Y: =472
        ZIndex: =3

